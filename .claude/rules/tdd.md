# TDD 开发规则

## 核心原则
- **没有失败的测试，不写生产代码**
- 每个 TDD 循环只聚焦一个行为
- 重构阶段不改变外部行为

## 红-绿-重构循环
1. **红**：写一个最小的失败测试 → `cargo test` 确认失败
2. **绿**：写刚好让测试通过的最少代码 → `cargo test` 确认通过
3. **重构**：优化代码 → `cargo test` 确认仍通过

## 禁止事项
- 禁止跳过红灯步骤直接写代码
- 禁止一次写多个测试再去实现
- 禁止在重构阶段添加新功能
- 禁止在测试未通过时开始新的循环