App:
  Name: xone.demo.app
  Version: v0.1.0

XLog:
  Level: info
  Format: json

XTrace:
  SampleRatio: 1
  ForwardHeaders:           # 只收 XGin.TrustedProxies 里的对端发来的值，默认谁都不信
    - X-Request-Id

XMetric:
  Namespace: demo
  ConstLabels:
    env: "${ENV:dev}"

XGin:
  Port: 8080
